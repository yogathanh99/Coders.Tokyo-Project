form(action="/users/search", method="GET")
  input(type="text" name="q" placeholder="Input here" value=q)
  button Search
a(href="/users/create") Add name
h1 Users:
ul
  each user in users
    li
      span=user.name
      span &nbsp
      a(href=`/users/${user.id}`) View